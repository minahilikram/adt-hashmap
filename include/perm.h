#include "linkedList.h"

/* Preconditions: A list exists.
Postconditions: Permutations are added to the list. */
void perm(char *a, int i, node *myList);

/* Preconditions: A string exists.
Postconditions: All elements in the string are lowercase. */
void toLowerCase(char *input);

int inputValidation(char *input);
